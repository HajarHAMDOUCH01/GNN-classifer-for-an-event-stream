to do : 
 * edge case : if t6 didn't exist how the model can learn to find an alignement 
   for a trace like : t1, t2, t1, t3, ... , it has to finish the process .. ? 
 * do batching with masking   => doesn't work! think of another solution 
 * fix code -> pytorch things => again , solve the parallelism problem 
 * think about edge cases 
 * !!!! check if it works without indexing transitions and places
 * !!!! think how to add the equation for constraining the loss function, 
        this solves the problem of how many times a transition is taken in the sequence 
        because the loss function should check transition by transition and target 
        and whether the same transition fires too many times
        and whether the total firing counts match a valid x satisfying m = m0 + C*x , 
        but this also doesn't mean the path found based on a x that satisfies the equation is a valid path , 
        only the other way : a valid path means there is an x that satisfies the equation 
        but this is trained with cross entropy anyway , but think about something better
 * think about the input of the model for the source and target marking !
 * why do you think you can estimate easily the target marking -> check it's always correct
 * find a way to find the matrix rotation in the same way between training and inference 

 * to think about : does it generalize ... , depends on what data to generate , what split to do , put edge cases in graphs -> tests

 done : 
  * test with different process graphs from different datasets (only for small graphs)
  * 